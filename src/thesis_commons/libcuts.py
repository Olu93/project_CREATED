import tensorflow as tf
# import keras.api._v2.keras as keras
# from keras.api._v2.keras import backend, optimizers, layers, models, losses, metrics, utils
# K = backend
from tensorflow import keras
from tensorflow.keras import  optimizers, layers, models, losses, metrics, utils, backend as K
